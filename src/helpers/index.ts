import randomANumberInARange from './random-a-number-in-a-range';
import validatePayloadAndThrowIfNotValid from './validate-payload-and-throw-if-not-valid.helper';

const helpers = {
  validatePayloadAndThrowIfNotValid,
  randomANumberInARange,
};

export default helpers;
