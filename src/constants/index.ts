import ERROR_MESSAGE from './error-message.constant';
import PAGINATION from './pagination.constant';
import TEST_USER from './test-user.constant';
import TOKEN_SETTING from './token-setting.constant';
import USER_ROLE from './user-role.constant';

const constants = {
  ERROR_MESSAGE,
  TOKEN_SETTING,
  USER_ROLE,
  PAGINATION,
  TEST_USER,
};

export default constants;
